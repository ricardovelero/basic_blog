<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="relative flex flex-wrap items-center content-between py-3 px-4 navbar-inverse">
      <div class="container mx-auto sm:px-4">
        <div class="navbar-header">
          <%= link_to 'Blog', root_path, class: 'inline-block pt-1 pb-1 mr-4 text-lg whitespace-no-wrap' %>
        </div>
        <div id="navbar">
          <ul class="flex flex-wrap list-none pl-0 mb-0 flex flex-wrap list-reset pl-0 mb-0">
            <li><%= link_to 'Home', root_path %></li>
          </ul>
          <ul class="flex flex-wrap list-none pl-0 mb-0 flex flex-wrap list-reset pl-0 mb-0 pull-right">
            <% if current_user %>
              <li><span><%= current_user.email %></span></li>
              <li><%= link_to 'Sign Out', sign_out_path, method: :delete %></li>
            <% else %>
              <li><%= link_to 'Sign In', sign_in_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container mx-auto sm:px-4">
      <% flash.each do |key, value| %>
        <div class="relative px-3 py-3 mb-4 border rounded alert-<%= key %>">
          <%= value %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
